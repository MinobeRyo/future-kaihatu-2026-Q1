コア設計
レシートをOCR（Google Vision API）で読み取り、品目テキストを抽出して分類し、ゲームに反映する。

成長値
メインステータスは成長値で、植物の成長段階とスケールに直結する。成長は４段階　500 1500 3000 5000以上

成長値は大体10〜50 MAXで2倍バフ１００ずつ上がるような設計にしたい

属性値
健康値 低下すると成長が停止。一定以下で枯れ木状態に移行。高いと成長値にバフ。50以下で枯れ木状態で100以上でバフがかかり、
0〜150までで変化する　100〜150までで0.5パーセントずつ　計1.25倍のバフになります
精神値 低いと入力される成長値が下がる。高いと成長値にバフ。時間経過で自然減少する。レシート撮ってから２４時間に１２減少　
0〜150まで変化する。０.５倍から1.25倍までで変化します。　要は×50%から125% １あたり0.５%ずつ上がります。 アプリを開いたときに前回開いてから何時間経ったかで計算する
バフ Nスキャン分の成長速度UPとして機能する。
1.1倍から1.5倍バフかかります

娯楽バフ（回数制）
* 娯楽レシートスキャンで回数が増える（足し算）
* 最大バフ×1.5まで足される（バフ値を足し算）
* 娯楽以外のレシートスキャン時に回数を1消費、バフ値（固定）を適用
* 回数が0になったらバフ値もリセット
* 娯楽バフ累積値 = スキャンごとのバフ値を加算 上限 1.5 で切り捨て（超過分は切り捨て） 例） 軽度(1.15) + 中度(1.30) = 1.45 　→ さらに軽度(1.15)スキャン = 1.60 → 1.5に切り捨て
アイテム取得時
* レシートスキャン時にバフ回数消費
* バフ込みの値でストックに保存
ストックは10枠・個別管理（同名でも数値が違うので別物扱い）
ストック満杯時は通常レシートのスキャン不可
娯楽レシートだけは例外でスキャン可（バフ稼ぎ用、アイテム生成しないので枠を消費しない）
これで仕様確定です。一点だけ補足しておくと、UIには**「あと◯枠」表示と、満杯時に「アイテムを使ってください」というガイド**を出す
重複レシート検知の方法 日付＋店名＋合計金額で判定


アイテム使用時
* ホーム画面で自由なタイミングで使用
* 使用時の精神値・健康値を参照してバフ適用
* 最大×1.5
最大倍率：×2.25  【スキャン時】 アイテム保存値 = (基礎成長値 + ジャンクボーナス) × 娯楽バフ 
【アイテム使用時】 成長値加算 = アイテム保存値 × {1 + 健康値バフ + 精神値バフ} 最大倍率 = ×1.5 × ×1.5 = ×2.25
 詳しくは 最終成長値 = (基礎成長値 + ジャンクボーナス)
           × 娯楽バフ                       ← 最大 ×1.5
           × {1 + (健康値バフ分 + 精神値バフ分)}  ← 最大 1 + 0.25 + 0.25 = ×1.5
なので最大倍率 = 1.5 × 1.5 = ×2.25。 
アイテム値はスキャン時に決定してアイテムとして入手。使用時バフはその後の使用時にかかる形

アイテムカテゴリと効果
食品（健康系） 健康値が大きく上がり、成長値は控えめに上がる。
食品（ジャンク系） 健康値が下がる代わりに成長値が大きく上がる。
日用品 精神値が上がり、少し成長値も上がる。

種類による差分
* 消耗品（シャンプー・洗剤・ティッシュなど）→ 精神値↑小・成長値↑小
* 文房具 → 精神値↑小・成長値↑小
* 家具・インテリア → 精神値↑大・成長値↑中
* 衣類 → 精神値↑中・成長値↑小

娯楽品 健康値が下がり、精神値と成長値にバフがかかる。
* 健康値↓・精神値↑は共通
* 疲れの強度によってマイナス幅が変わる
強度の分け方（仮）
* 軽度：映画、読書カフェなど → 健康値↓小・精神値↑大
* 中度：カラオケ、ゲームセンターなど → 健康値↓中・精神値↑中
* 重度：テーマパーク、スポーツ観戦など → 健康値↓大・精神値↑大

ジャンク品ボーナス仕様
カテゴリ	健康値	精神値	成長値	レシートバフ値	バフ回数
食品（健康系）野菜・肉・魚	+15~25	±0	+30~50	±0	
食品（その他）	+5~8	±0	+10~20	±0	
食品（ジャンク系）	-15~-25	±0	+10~20 （ボーナス）	±0	
日用品（消耗品・文房具）	+1~5	+5~8	+10	±0	
日用品（家具・インテリア）	+1~5	+20~25	+15	±0	
日用品（衣類）	+1~5	+10~15	+10	±0	
娯楽（軽度）	-5~-8	+5~15	±0	1.1~1.2	+２
娯楽（中度）	-10~-15	+10~15	±0	1.25~1.35	＋３
娯楽（重度）	-20~-25	+20~25	±0	1.35~1.5	＋4
* 個数・量が多いほど+10〜+20のボーナスが成長値に加算
* 少量なら+10寄り、多いほど+20に近づく
娯楽仕様
* 重度は「のめり込んで楽しい・しかし疲労から健康を損なう」設計
* 重度は回数制のため、1枚で上限バフ1.5到達は許容範囲
レシートごとの合計から属性値が振れてアイテム個数からアイテムの種類と数値が決定
その中に飲食物にジャンク系の品物があるだと特別枠で加算が入る。
単体でも+10~20 （ボーナス）

ゲームの戦略軸
健康値・精神値どちらも高い状態を維持しつつ、お菓子などの娯楽系アイテムによるマイナス効果とバフをうまく活用して、効率よく成長値を伸ばすのが目標となる。


食品分類
野菜・肉・魚は健康値が大きく上がり、成長値も中程度上がる。お菓子・ジュースは健康値が下がる代わりに成長値が大きく上がる。その他食品は健康値・成長値ともに小幅上昇。

アイテム
レシートの内容に応じてドロップする。すべてトレードオフ設計。ストックは10個まで。満杯時は新たなアイテムは取得できないが、バフのみ発動する。
これは満杯だとアイテムが取得できないが、娯楽レシートのバフはアイテムではなくそもそもその場発動だからってこと
またレシート一個につき一個アイテムが出てきて数や物に応じて上のカテゴリから数値が決定される。

植物
成長段階は成長値をベースに4段階。苗木・幼木・青木・花咲きの順に画像を差し替える。健康値が一定以下になると枯れ木画像に強制差し替えされ、回復すると元の段階に戻る。

植物の見た目
段階の切り替えは画像差し替えで対応する。段階内の細かい成長表現はCSSのscaleで微調整する。
 supabaseについて
Google Vision APIキーとGemini APIキーをReact Nativeアプリのバンドルに直接入れると抜かれるため。Supabase Edge Functions（Deno）を1枚挟んで、そこからVision/Geminiを叩く構成



分析画面のペルソナ生成 
支出比率のルールベースで分岐


パイプライン
レシート撮影（Expo Camera）
　↓
Google Vision API（OCR）
　↓
Gemini API（品目分類）← ここだけ変更
　↓
Supabase（DB）
技術スタック
React native
View
TypeScript StyleSheet  （CSSの書き方をJavaScriptオブジェクト形式に変換したものみたいなやつ） 
更新版画面一覧
ログイン画面
↓
1. ホーム画面
* 植物表示（中央）
* 上部：成長値・健康値・精神値・バフ状態
* アイテムストック表示＋使用操作
* 下部：ナビゲーションバー
2. レシートスキャン画面
* カメラ起動
* OCR → Gemini分類
* 結果モーダル表示（分類結果・属性値変動・取得アイテム）
3. 分析画面
* 月次支出グラフ
* 支出比率からペルソナ生成
* 生活バランススコア
* エコスコア＋改善提案
4. 設定画面
最低限
* プロフィール（名前・アイコン）
* 通知設定
* データリセット
あると面白い
* 植物の名前をつける
* テーマ・カラー変更

Web	React Native
HTML要素（div, p, span）	コンポーネント（View, Text, Image）
CSS	StyleSheet（書き方はCSSに似てる）
ブラウザで動く	iOSとAndroidで動く
index.html	App.js がエントリーポイント

認証確定
* メール＋パスワード（Supabase Auth）
* 初期実装はこれだけでOK、後から追加可能

📋 仕様確定サマリー
項目	確定内容
認証	メール＋パスワード
OCR	Google Vision API（Edge Functions経由）
分類	Gemini API（Edge Functions経由）
DB	Supabase
重複検知	日付＋店名＋合計金額
ストック	10枠・個別管理
娯楽バフ	加算・上限1.5で切り捨て
計算式	スキャン時確定・使用時バフ適用・最大×2.25
ペルソナ	ルールベース分岐
植物	4段階・画像差し替え＋scaleで微調整

確定DBスキーマ
profiles


user_id, name, avatar_url, plant_name, created_at
plant_status（履歴積み上げ）


id, user_id, growth_value, health_value, mental_value,
changed_at, trigger_type
// trigger_type: receipt_scan / item_use / time_decay
entertainment_buff


user_id, buff_value（上限1.5）, buff_count（残回数）
receipts


id, user_id, store_name, receipt_date, total_amount,
scanned_at, is_duplicate, entertainment_intensity,
food_healthy_ratio, food_junk_ratio, food_other_ratio,
daily_goods_ratio, entertainment_ratio
receipt_items


id, receipt_id, item_name, category, quantity, price
item_stock


id, user_id, item_name, category,
stored_growth_value, health_effect, mental_effect,
acquired_at
